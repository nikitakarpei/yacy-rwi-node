// Package nats carries the end of a scrape to whoever waits for that one page: the failure
// the service settles on, and every receipt a corpus sends back, each on the feed subject of
// the page. Nothing keeps a message: a listener that is away when one is published never
// learns it. A receipt is carried over as it stands, so what a corpus reports is never
// interpreted here.
package nats
